import 'package:flutter/material.dart';

class CenterCrosser extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Icon(Icons.add, size: 40, color: Colors.white));
  }
}
